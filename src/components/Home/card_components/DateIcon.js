import React from "react";
import calendar from "../../../icons/calendar.svg";

const iconSize = 24;
const DateIcon = () => {
  return <img src={calendar} width={iconSize} height={iconSize} alt="add" />;
};

export default DateIcon;
